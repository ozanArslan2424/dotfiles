-- "rounded" or "double"
BorderStyle = require("global.border_style").setup(BorderStyleOption)

Do = {
	hl = require("global.hl"),
	map = require("global.map"),
	combine = require("global.combine"),
	enable_lsp = require("global.enable_lsp"),
	auto_cmd = require("global.auto_cmd"),
	user_cmd = require("global.user_cmd"),
	apply_config = require("global.apply_config"),
	search_for_in_cwd = require("global.search_for_in_cwd"),
}
